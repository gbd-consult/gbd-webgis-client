import ol from 'ol-all';

class TMap extends ol.Map {
    constructor() {
        super({
            controls: []
        });


    }
}

let _map = null;

export function get() {
    if (_map)
        return _map;

    _map = new TMap();
    if(window)
        window._map = _map;

    return _map;
}

