import ol_Map from 'ol/map';

class TMap extends ol_Map {
    constructor() {
        super({
            controls: []
        });


    }
}

let _map = null;

export function get() {
    if (_map)
        return _map;

    _map = new TMap();
    if(window)
        window._map = _map;

    return _map;
}

